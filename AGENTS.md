# Archived external mirror

This repository is read-only and has no BFLD operational authority.

For BFLD work, begin at `/Users/antonaci/BFLD/.agent-os/AGENTS.md` and resolve
repositories through the Registry. Do not load `docs/history/` unless an
explicit provenance task requires it.
