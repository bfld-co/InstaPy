# Archived external mirror

This organization fork is retained only for provenance and recovery. Its
upstream project and full imported snapshot remain available through Git
history; the prior working tree is also preserved under `docs/history/`.

It has no BFLD runtime, product, method, brand or governance authority.
